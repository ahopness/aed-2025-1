Sorts presentes
    void bubble_sort(int arr[], int n);
    void insertion_sort(int arr[], int n);
    void merge_sort(int arr[], int left, int right);
    void quick_sort(int arr[], int low, int high);
    void heap_sort(int arr[], int n);
    void bogosort(int arr[], int n);

por enquanto só funciona com inteiros mas vou testar se o Auto deve ajudar pra criar uma função generica
oque vai ajudar uma vez se precisar de float vai funcionar do mesmo jeito, mas se o auto não funcionar eu
vejo como colocar pra funcionar com float e int.

